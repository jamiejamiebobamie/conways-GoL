class SnapshotHolder{
    constructor(snapshots, widthOfCanvas, heightOfCanvas){
        this.snapshots = []
    }

    // p5.js built-in method
    draw () {
        for (let i = 0; i < snapshots.length; i++){
            // snapshot.draw(i);
        }
    }

}
